server:
  port: 10086
spring:
  application:
    name: eureka-server
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    #register-with-eureka: false
    #fetch-registry: false
  server:
    eviction-interval-timer-in-ms: 10 #生产时不需要修改   开发时需要修改
    enable-self-preservation: false #开发时关闭自我保护模式   默认为打开